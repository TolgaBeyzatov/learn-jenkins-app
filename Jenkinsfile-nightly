pipeline {
    agent any


    stages {
        // This is a comment
        /*
        line 1 
        line 2
        */
        stage('Build Docker images') {
            steps {
                 sh 'docker build -f ci/Dockerfile-playwright -t playwright-image .' 
                 sh 'docker build -f ci/Dockerfile-aws-cli -t my-aws-cli .' 
            }           
        }
    }
}    